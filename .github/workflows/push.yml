name: Push Exercices

on:
  push:
    branches:
      - 'dev'
      - 'staging'
      - 'feat**'
    paths:
      - '**.ts'

jobs:
  echo-smtg:
    runs-on: ubuntu-latest

    steps:
      - run: echo MY COMMAND IS PRINTED
