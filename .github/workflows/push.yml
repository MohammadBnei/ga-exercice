name: Push Exercices

on:
  push:
    # branches:
    #   - 'dev'
    #   - 'staging'
    #   - 'feat**'
    path:
      - '**.ts'

jobs:
  echo-smtg:
    runs-on: ubuntu-latest

    steps:
      - run: echo MY COMMAND IS PRINTED
