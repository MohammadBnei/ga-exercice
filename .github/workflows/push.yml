name: Push Exercices

on:
  push:
    branches:
      - 'dev'
      - 'staging'
      - 'feat**'
    paths:
      - '**.ts'
      - '$my-folder/*'

jobs:
  echo-smtg:
    runs-on: ubuntu-latest

    steps:
      - run: echo MY COMMAND IS PRINTED
