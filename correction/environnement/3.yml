name: Joke app environnement (staging)

on:
  push:
    branches:
      - staging

jobs:
  build-test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: joke-app

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 16
      - name: Install dependencies
        run: |
          npm ci
          npm test
          npm run build

  deploy:
    runs-on: ubuntu-latest
    needs: build-test

    environment: 
      name: staging
      url: https://joke-app-staging.herokuapp.com

    steps:
      - uses: actions/checkout@v3

      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: ${{secrets.HEROKU_API_KEY}}
          heroku_app_name: "joke-app-staging" #Must be unique in Heroku
          heroku_email: "mohammadamine.banaei@protonmail.com"
          appdir: joke-app
          # secrets.JOKE_URL will be inside the environnement (staging) secrets
          procfile: "web: JOKE_URL=${{secrets.JOKE_URL}} npm run start:prod"
